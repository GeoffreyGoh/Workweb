/* =====================================================================
   Deployment config. Points the frontend at the backend's API origin.
   Loaded before api.js on every page.
   ===================================================================== */
window.API_BASE_URL = "https://workweb-backend-x7q2.onrender.com";
